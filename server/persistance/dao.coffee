

module.exports =
	###
        Takes a single ID or an array of IDs
        Returns a single model or an array of models accordingly
	###
	getModel: (id) ->

	saveModel: (id) ->

	deleteModel: (id) ->